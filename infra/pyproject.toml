[project]
name = "tigergoat-infra"
version = "0.1.0"
description = "Cloudflare DNS + Pages custom domain for tigergoat.tomv.uk"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3,<4",
    "pulumi-cloudflare>=5,<6",
]

[tool.uv]
package = false
